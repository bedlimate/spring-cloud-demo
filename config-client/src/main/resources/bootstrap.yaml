spring:
  application:
    name: client
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:9000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
server:
  port: 9005

